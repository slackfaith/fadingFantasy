program project1;
{$mode objfpc}{$H+}

uses

  {$IFDEF HAS_TYPES}
    Types{$IFNDEF NDS},
  {$ENDIF}

  {$IFDEF WINDOWS}
    Windows,
  {$ENDIF}

  {$IFDEF UNIX}
    {$IFDEF FPC}
    pthreads,
    unixtype,
    baseunix,
    {$IFNDEF GP2X}
    unix,
    {$ELSE}
    unix,
    {$ENDIF}
    {$IFNDEF GP2X}
    x,
    xlib
    {$ENDIF}
    {$ELSE}
    Libc,
    Xlib,
    {$ENDIF}
  {$ENDIF}
 {$ENDIF}

 rpg_version, SDL_app,
  sysutils, lua, lauxlib, lualib;

{$R *.res}

begin
  WriteLn('GAME: ' + VersionString);
  SDL_TEST;
end.

